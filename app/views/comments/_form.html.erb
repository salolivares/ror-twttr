<%= form_with(model: [post, post.comments.build]) do |f| %>
  <div class="form-control">
    <%= f.rich_text_area :body %>
    <%= f.submit "Reply",
             class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium mt-2" %>
  </div>
<% end %>
